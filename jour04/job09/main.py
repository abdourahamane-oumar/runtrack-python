def my_function(liste):

  Max = max(liste) #Utilise la méthode max() pour récuperer la valeur la plus grande du tableau
  Min = min(liste) #Utilise la méthode min() pour récuperer la valeur la plus petit du tableau
  return Min , Max #Renvoie la variable
L = [8, 24, 27, 48, 2, 16, 9, 102, 7, 84, 91]

Max = max(L) 
Min = min(L)

# MAX=haut(L) #Appel de fonction
# MIN=bas(L) #Appel de fonction

print("La valeur max est : ",Max)
print("La valeur min est: ",Min) 


